[project]
name = "knowledge-demo"
version = "0.1.0"
description = "HR Benefits Assistant — agent-level and session-level knowledge demo"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12, <3.14"
dependencies = [
    "cuga",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
cuga = { path = "../../../", editable = true }
